//Satlite should be changed to being the base class, but for now planet is fine
import { Planet } from "./planet";
  
export class Satlite extends Planet {
  constructor(props) {
    super(props);
  }
}
  